<!DOCTYPE html>
<html lang="ru" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кофейня "Уют"</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .hero-section {
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1501339847302-ac426a4a7cbb?ixlib=rb-4.0.3&auto=format&fit=crop&w=1950&q=80');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 150px 0;
            text-align: center;
        }
        .product-card {
            transition: transform 0.3s;
            margin-bottom: 20px;
        }
        .product-card:hover {
            transform: translateY(-5px);
        }
        .navbar-brand {
            font-weight: bold;
            font-size: 1.5rem;
        }
        .coffee-icon {
            color: #8B4513;
        }
    </style>
</head>
<body>
<!-- Навигация -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">
            <i class="fas fa-coffee coffee-icon"></i> Кофейня "Уют"
        </a>
        <div class="navbar-nav">
            <a class="nav-link" href="/">Главная</a>
            <a class="nav-link" href="/menu">Меню</a>
            <a class="nav-link" href="/about">О нас</a>
            <a class="nav-link" href="/order">Заказать</a>
        </div>
    </div>
</nav>

<!-- Герой секция -->
<section class="hero-section">
    <div class="container">
        <h1 class="display-4 fw-bold">Добро пожаловать в кофейню "Уют"</h1>
        <p class="lead">Лучший кофе в городе с 2010 года</p>
        <a href="/menu" class="btn btn-primary btn-lg mt-3">
            <i class="fas fa-utensils"></i> Посмотреть меню
        </a>
    </div>
</section>

<!-- Преимущества -->
<section class="py-5">
    <div class="container">
        <div class="row text-center">
            <div class="col-md-4">
                <i class="fas fa-coffee fa-3x coffee-icon mb-3"></i>
                <h4>Свежий кофе</h4>
                <p>Ежедневно обжариваем зерна арабики</p>
            </div>
            <div class="col-md-4">
                <i class="fas fa-clock fa-3x text-warning mb-3"></i>
                <h4>Быстрая доставка</h4>
                <p>Доставляем за 30 минут или кофе бесплатно</p>
            </div>
            <div class="col-md-4">
                <i class="fas fa-heart fa-3x text-danger mb-3"></i>
                <h4>С любовью</h4>
                <p>Каждая чашка готовится с заботой о вас</p>
            </div>
        </div>
    </div>
</section>

<!-- Популярные товары -->
<section class="py-5 bg-light">
    <div class="container">
        <h2 class="text-center mb-5">Популярные напитки</h2>
        <div class="row">
            <div th:each="product : ${featured}" class="col-md-4">
                <div class="card product-card">
                    <img th:src="${product.image}" class="card-img-top" style="height: 200px; object-fit: cover;">
                    <div class="card-body">
                        <h5 class="card-title" th:text="${product.name}"></h5>
                        <p class="card-text" th:text="${product.description}"></p>
                        <p class="h5 text-primary" th:text="'₽' + ${product.price}"></p>
                        <a th:href="@{/order}" class="btn btn-primary mt-2">
                            <i class="fas fa-shopping-cart"></i> Заказать
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Футер -->
<footer class="bg-dark text-white py-4">
    <div class="container text-center">
        <p>&copy; 2025 Кофейня "Уют". Все права защищены.</p>
        <p>📞 +7 (999) 123-45-67 | 📍 ул. Свободы, 2В</p>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>